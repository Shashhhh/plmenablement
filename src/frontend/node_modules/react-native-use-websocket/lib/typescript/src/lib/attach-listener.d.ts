/// <reference types="react-native" />
import type { MutableRefObject } from 'react';
import { ReadyState } from './constants';
import type { Options, WebSocketEventMap } from './types';
export interface Setters {
    setLastMessage: (message: WebSocketEventMap['message']) => void;
    setReadyState: (readyState: ReadyState) => void;
}
export declare const attachListeners: (webSocketInstance: WebSocket, setters: Setters, optionsRef: MutableRefObject<Options>, reconnect: () => void, reconnectCount: MutableRefObject<number>) => (() => void);
