const subscribers = {};
export const getSubscribers = url => {
  return Array.from(subscribers[url]);
};
export const hasSubscribers = url => {
  return subscribers[url].size > 0;
};
export const addSubscriber = (url, subscriber) => {
  subscribers[url] = subscribers[url] || new Set();
  subscribers[url].add(subscriber);
};
export const removeSubscriber = (url, subscriber) => {
  subscribers[url].delete(subscriber);
};
//# sourceMappingURL=manage-subscribers.js.map